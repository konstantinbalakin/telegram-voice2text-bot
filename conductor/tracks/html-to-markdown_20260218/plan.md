# Implementation Plan: Переход форматирования с HTML на MarkdownV2

**Track ID:** html-to-markdown_20260218
**Spec:** [spec.md](./spec.md)
**Created:** 2026-02-18
**Status:** [ ] Not Started

## Overview

Переводим систему форматирования текста с HTML-тегов на Markdown-разметку. LLM будет генерировать Markdown, текст хранится в Markdown, отправляется в Telegram с `parse_mode="MarkdownV2"`. Для PDF-экспорта добавляем промежуточную конвертацию MD→HTML перед WeasyPrint.

## Phase 1: Утилиты — MarkdownV2 экранирование и конвертация

Создание базовых утилит, которые понадобятся всем остальным фазам.

### Tasks

- [x] Task 1.1: Написать тесты для функции `escape_markdownv2(text)` — экранирование спецсимволов MarkdownV2 (`_ * [ ] ( ) ~ \` > # + - = | { } . !`) в обычном тексте, с сохранением Markdown-разметки (не экранировать `**bold**`, `*italic*`, `` `code` ``)
- [x] Task 1.2: Написать тесты для функции `sanitize_markdown(text)` — очистка текста от возможных остатков HTML-тегов (fallback на случай, если LLM вставит HTML)
- [x] Task 1.3: Написать тесты для функции `strip_markdown(text)` — удаление Markdown-разметки для получения plain text с сохранением эмодзи
- [x] Task 1.4: Реализовать `src/utils/markdown_utils.py` с функциями `escape_markdownv2()`, `sanitize_markdown()`, `strip_markdown()`
- [x] Task 1.5: Убедиться, что все тесты фазы 1 проходят

### Verification

- [ ] Все unit-тесты для markdown_utils проходят
- [ ] `escape_markdownv2("Привет *мир*! Цена: 100$.")` — корректно экранирует `.` и `!`, но сохраняет `*мир*`
- [ ] `strip_markdown("**жирный** и *курсив*")` → `"жирный и курсив"`
- [ ] `strip_markdown("текст 🎉 с эмодзи")` → `"текст 🎉 с эмодзи"` (эмодзи сохраняются)

## Phase 2: Обновление LLM-промптов

Переписать инструкции по форматированию с HTML на Markdown во всех промптах.

### Tasks

- [x] Task 2.1: Обновить `prompts/structured.md` — заменить секцию "Форматирование" с HTML-тегов на Markdown-синтаксис (`**bold**`, `*italic*`, `` `code` ``, `` ```code block``` ``). Ничего другого кроме html-тегов не менять!
- [x] Task 2.2: Обновить `prompts/magic.md` — заменить HTML-инструкции на Markdown
- [x] Task 2.3: Обновить `prompts/summary.md` — заменить HTML-инструкции на Markdown
- [x] Task 2.4: Обновить `prompts/refinement.md` — заменить HTML-инструкции на Markdown
- [x] Task 2.5: Обновить `prompts/length_shorter.md` — изменить "Сохранить HTML-форматирование" на "Сохранить Markdown-форматирование"
- [x] Task 2.6: Обновить `prompts/length_longer.md` — изменить "Сохранить HTML-форматирование" на "Сохранить Markdown-форматирование"

### Verification

- [ ] Ни один промпт-файл не содержит упоминаний HTML-тегов для форматирования
- [ ] Все промпты содержат инструкции по Markdown-разметке

## Phase 3: Обновление сервисов — text_processor и transcription_orchestrator

Заменить вызовы `sanitize_html()` и `escape_html()` на аналоги из markdown_utils.

### Tasks

- [x] Task 3.1: Написать/обновить тесты для `text_processor.py` с учётом Markdown-выхода от LLM
- [x] Task 3.2: Обновить `src/services/text_processor.py` — заменить `sanitize_html` на `sanitize_markdown` (5 мест)
- [x] Task 3.3: Написать/обновить тесты для `transcription_orchestrator.py` с учётом Markdown-форматирования
- [x] Task 3.4: Обновить `src/services/transcription_orchestrator.py` — заменить `sanitize_html`/`escape_html` на `sanitize_markdown`/`escape_markdownv2`
- [x] Task 3.5: Убедиться, что все тесты фазы 3 проходят

### Verification

- [ ] `text_processor.py` не импортирует `html_utils`
- [ ] `transcription_orchestrator.py` использует `markdown_utils`
- [ ] Все unit-тесты проходят

## Phase 4: Обновление Telegram-обработчиков — parse_mode="MarkdownV2"

Переключить все точки отправки сообщений в Telegram с HTML на MarkdownV2.

### Tasks

- [x] Task 4.1: Написать/обновить тесты для `callbacks.py` с `parse_mode="MarkdownV2"`
- [x] Task 4.2: Обновить `src/bot/callbacks.py` — заменить все `parse_mode="HTML"` на `parse_mode="MarkdownV2"`, заменить `sanitize_html` на `escape_markdownv2`
- [x] Task 4.3: Написать/обновить тесты для `handlers.py` с `parse_mode="MarkdownV2"`
- [x] Task 4.4: Обновить `src/bot/handlers.py` — заменить `parse_mode="HTML"` на `parse_mode="MarkdownV2"`
- [x] Task 4.5: Убедиться, что все тесты фазы 4 проходят

### Verification

- [ ] Ни один файл в `src/bot/` не содержит `parse_mode="HTML"` (кроме, возможно, обратной совместимости)
- [ ] Все handler-тесты проходят

## Phase 5: Обновление PDF-генератора — Markdown → HTML → WeasyPrint

Добавить конвертацию Markdown в HTML перед рендерингом PDF.

### Tasks

- [x] Task 5.1: Написать тесты для конвертации Markdown → HTML в PDFGenerator (заголовки, bold, italic, code, списки)
- [x] Task 5.2: Обновить `src/services/pdf_generator.py` — добавить метод `convert_markdown_to_html()` с использованием библиотеки `markdown` или кастомных regex
- [x] Task 5.3: Обновить метод `generate_pdf()` — вызывать `convert_markdown_to_html()` перед рендерингом
- [x] Task 5.4: Убедиться, что PDF корректно рендерит Markdown-форматирование

### Verification

- [ ] PDF содержит корректное форматирование (жирный, курсив, списки)
- [ ] Все тесты PDFGenerator проходят

## Phase 6: Обновление ExportService и финальная верификация

Проверить/обновить ExportService и провести финальную верификацию всех форматов.

### Tasks

- [x] Task 6.1: Обновить тесты ExportService — убедиться, что входные данные теперь в Markdown
- [x] Task 6.2: Обновить `export_service.py` — метод `export_txt()` должен использовать `strip_markdown()` из `markdown_utils` для корректной очистки, включая сохранение эмодзи
- [x] Task 6.3: Проверить, что `export_md()` возвращает валидный Markdown (теперь работает корректно, т.к. вход — Markdown)
- [x] Task 6.4: Проверить, что `export_docx()` корректно парсит Markdown (уже работает)
- [x] Task 6.5: Удалить или пометить как deprecated `src/utils/html_utils.py` (если не используется нигде)
- [x] Task 6.6: Запустить полный набор тестов и линтеров: `ruff check`, `black --check`, `mypy`, `pytest`

### Verification

- [ ] `ruff check src/` — без ошибок
- [ ] `black --check src/ tests/` — без ошибок
- [ ] `mypy src/` — без ошибок
- [ ] `TELEGRAM_BOT_TOKEN=test pytest tests/unit/ -v` — все тесты проходят

## Final Verification

- [ ] Все acceptance criteria из spec.md выполнены
- [ ] MD-файл содержит валидную Markdown-разметку
- [ ] TXT-файл содержит чистый текст с эмодзи (если были) без форматирования
- [ ] PDF корректно отображает форматирование
- [ ] DOCX корректно отображает форматирование
- [ ] Telegram-сообщения корректно отображаются с MarkdownV2
- [ ] Все тесты и линтеры проходят
- [ ] Коммиты сделаны в ветке `feature/download-formats`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
