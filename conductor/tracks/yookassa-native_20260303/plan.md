# Implementation Plan: YooKassa Native Telegram Payments Integration

**Track ID:** yookassa-native_20260303
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-03
**Status:** [ ] Not Started

## Overview

Заменить `YooKassaProvider` (прямой API) на нативную интеграцию через Telegram Payments API. Новый провайдер будет работать идентично `TelegramStarsProvider`, но с `currency="RUB"` и `provider_token` от ЮКассы. Удалить старые env-переменные и зависимости.

## Phase 1: Config & Provider Refactor

Обновить конфигурацию и переписать YooKassa-провайдер.

### Tasks

- [x] Task 1.1: Обновить `config.py` — заменить `yookassa_shop_id`, `yookassa_secret_key`, `yookassa_return_url` на `yookassa_provider_token: str = ""`
- [x] Task 1.2: Обновить `.env.example` и `.env.example.short` — заменить 3 переменные на `YOOKASSA_PROVIDER_TOKEN=`
- [x] Task 1.3: Обновить `.github/workflows/deploy.yml` — заменить env-переменные
- [x] Task 1.4: Написать тесты для нового `YooKassaProvider` (TDD: red)
- [x] Task 1.5: Переписать `src/services/payments/yookassa_provider.py` — нативный Telegram Payments API (аналогично `TelegramStarsProvider`, но `currency="RUB"`, `provider_token`)
- [x] Task 1.6: Убрать пакет `yookassa` из зависимостей (pyproject.toml/uv.lock), если он есть

### Verification

- [ ] Тесты нового `YooKassaProvider` проходят
- [ ] `config.py` содержит только `yookassa_provider_token`
- [ ] Нет упоминаний `yookassa_shop_id`, `yookassa_secret_key`, `yookassa_return_url` в коде

## Phase 2: Wiring & Handlers

Подключить провайдер в `main.py` и обновить callback-хендлеры.

### Tasks

- [x] Task 2.1: Написать/обновить тесты для card-callback хендлеров в `payment_callbacks.py` (TDD: red)
- [x] Task 2.2: Обновить `main.py` — создать и зарегистрировать `YooKassaProvider` при наличии `yookassa_provider_token`
- [x] Task 2.3: Обновить `payment_callbacks.py` — `buy_package_card_callback` и `buy_subscription_card_callback` должны создавать реальные инвойсы через `PaymentService`
- [x] Task 2.4: Обновить/расширить `pre_checkout_query_handler` и `successful_payment_handler` для поддержки RUB-платежей (если нужно различать Stars и YooKassa)
- [x] Task 2.5: Убрать заглушки "Оплата картой скоро будет доступна" из UI

### Verification

- [ ] Тесты callback-хендлеров проходят
- [ ] `YooKassaProvider` зарегистрирован в `main.py`
- [ ] Кнопки "Оплата картой" создают инвойсы

## Phase 3: Cleanup & Final Verification

Удалить старый код, обновить тесты, финальная проверка.

### Tasks

- [ ] Task 3.1: Удалить старые тесты для прямого YooKassa API (если есть)
- [ ] Task 3.2: Убрать все упоминания `yookassa_shop_id`, `yookassa_secret_key`, `yookassa_return_url` из кодовой базы
- [ ] Task 3.3: Проверить и обновить Alembic миграции (если модель `Purchase` ссылалась на старые поля)
- [ ] Task 3.4: Запустить полный CI-пайплайн: ruff + black + mypy + pytest

### Verification

- [ ] Нет упоминаний старых env-переменных в проекте
- [ ] Все 977+ тестов проходят
- [ ] mypy, ruff, black без ошибок

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Оплата картой работает через нативный Telegram Payments
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
