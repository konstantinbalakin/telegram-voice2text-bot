# Implementation Plan: Проброс user_id для персональных цен и отображение expires_at в балансе

**Track ID:** personal-prices-fix_20260307
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-07
**Status:** [x] Complete

## Overview

Проброс `user_id` через цепочку billing_commands → service → repository для персональных цен, и расширение `/balance` для отображения `expires_at`.

## Phase 1: Проброс user_id для персональных цен

Добавить параметр `user_id` в сервисные методы и передать его из UI-слоя.

### Tasks

- [x] Task 1.1: Написать тесты для `subscription_service.get_tier_prices(tier_id, user_id)` — проверить, что user_id пробрасывается в репозиторий
- [x] Task 1.2: Написать тесты для `payment_service.get_active_packages(user_id)` — проверить, что user_id пробрасывается в репозиторий
- [x] Task 1.3: Добавить параметр `user_id: Optional[int] = None` в `subscription_service.get_tier_prices()` и пробросить в `get_effective_prices()`
- [x] Task 1.4: Добавить параметр `user_id: Optional[int] = None` в `payment_service.get_active_packages()` и пробросить в `get_effective_packages()`
- [x] Task 1.5: В `billing_commands._build_subscriptions_catalog()` — получить DB user_id из telegram_user_id и передать в `get_tier_prices()`
- [x] Task 1.6: В `billing_commands._build_packages_catalog()` — получить DB user_id из telegram_user_id и передать в `get_active_packages()`
- [x] Task 1.7: Написать тесты для `_build_subscriptions_catalog` и `_build_packages_catalog` — проверить, что персональные цены возвращаются для нужного пользователя

### Verification

- [x] Все тесты проходят: `TELEGRAM_BOT_TOKEN=test uv run pytest tests/unit/ -v`
- [x] Линтеры чистые: `uv run ruff check src/ && uv run black --check src/ tests/ && uv run mypy src/`

## Phase 2: Отображение expires_at в /balance

Расширить `UserBalance` и UI для показа ближайшей даты истечения минут.

### Tasks

- [x] Task 2.1: Написать тесты для `billing_service.get_user_balance()` — проверить, что возвращаются поля `bonus_expires_at` и `package_expires_at`
- [x] Task 2.2: Добавить поля `bonus_expires_at: Optional[datetime]` и `package_expires_at: Optional[datetime]` в `UserBalance`
- [x] Task 2.3: В `billing_service.get_user_balance()` — получить активные балансы и вычислить ближайшие expires_at для bonus и package
- [x] Task 2.4: Написать тест для форматирования expires_at в `_build_balance_text_and_markup`
- [x] Task 2.5: В `billing_commands._build_balance_text_and_markup()` — отформатировать expires_at рядом с суммой минут: `🎁 Бонусные минуты: 45.0 мин (до 15.04.2026)`

### Verification

- [x] Все тесты проходят: `TELEGRAM_BOT_TOKEN=test uv run pytest tests/unit/ -v`
- [x] Линтеры чистые: `uv run ruff check src/ && uv run black --check src/ tests/ && uv run mypy src/`

## Final Verification

- [x] Все acceptance criteria из spec.md выполнены
- [x] Тесты проходят
- [ ] Ручное тестирование по тест-плану `docs/testing/billing-manual-test-plan.md` (TC-04, TC-05, TC-10)
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
