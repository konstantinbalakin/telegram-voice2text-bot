# Specification: Download Transcription in Multiple Formats

**Track ID:** download-formats_20260217
**Type:** Feature
**Created:** 2026-02-17
**Status:** Draft

## Summary

Добавить кнопку "📥 Скачать" к результатам транскрипции. По нажатию пользователь выбирает формат (MD, TXT, PDF, DOCX) и получает файл как документ в Telegram.

## Context

Бот уже поддерживает интерактивные кнопки для обработки текста (структурирование, резюме, "сделать красиво", смайлы, таймкоды, ретранскрипция). PDF-генерация через WeasyPrint уже реализована для файловых сообщений. Необходимо добавить явное скачивание в 4 форматах по запросу пользователя.

## User Story

Как пользователь бота, я хочу скачать результат транскрипции в удобном формате (MD, TXT, PDF, DOCX), чтобы сохранить его локально, переслать коллегам или использовать в документах.

## Acceptance Criteria

- [ ] После транскрипции (и после обработки текста, если применялась) появляется кнопка "📥 Скачать"
- [ ] По нажатию на "Скачать" отображается выбор формата: MD, TXT, PDF, DOCX
- [ ] TXT — чистый текст без форматирования (markdown-разметка удалена)
- [ ] MD — текст с оригинальным markdown-форматированием
- [ ] PDF — отформатированный документ (используется существующий WeasyPrint)
- [ ] DOCX — документ Word с базовым форматированием
- [ ] Файл отправляется пользователю как документ в Telegram

## Button Placement

Кнопка "📥 Скачать" располагается после всех кнопок обработки текста, перед кнопкой ретранскрипции:

1. Исходный текст
2. 📝 Структурировать
3. 📌 О чем текст?
4. 🪄 Сделать красиво
5. 😊 Смайлы
6. 🕐 Таймкоды
7. **📥 Скачать** ← новая кнопка
8. ⚡ Есть ошибки? Могу лучше!

## Dependencies

- **WeasyPrint** — уже в проекте, для PDF-генерации
- **python-docx** — новая зависимость для DOCX-генерации
- **Inline-кнопки** — существующая система в `src/bot/keyboards.py`

## Out of Scope

- Конвертация между форматами (только скачивание результата транскрипции)
- Настройка шаблонов/стилей PDF и DOCX пользователем
- Пакетное скачивание нескольких транскрипций в одном файле
- Хранение сгенерированных файлов на сервере (файлы создаются на лету и удаляются после отправки)

## Technical Notes

- Новые actions: `"download"` (показать меню форматов), `"download_fmt"` (скачать в формате)
- Feature flag: `enable_download_button: bool` в `config.py`
- Скачивается текущий активный вариант текста (с учётом active_mode, length_level, emoji_level, timestamps_enabled)
- Для DOCX: конвертация markdown → docx с базовым форматированием (заголовки, списки, параграфы)
- Подменю форматов заменяет основную клавиатуру; кнопка "◀ Назад" возвращает к основной

---

_Generated by Conductor. Review and edit as needed._
