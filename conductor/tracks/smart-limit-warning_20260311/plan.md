# Implementation Plan: Smart Limit Warnings

**Track ID:** smart-limit-warning_20260311
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-11
**Status:** [x] Complete

## Overview

Небольшое изменение в `transcription_orchestrator.py`: добавить проверку на наличие дополнительных минут (`package_minutes`, `bonus_minutes`) и на их основе выбирать текст предупреждения — мягкий (информационный) или текущий (пугающий).

## Phase 1: Tests

Написать unit-тесты для нового поведения предупреждений в orchestrator.

### Tasks

- [x] Task 1.1: В существующем тест-файле для orchestrator найти тесты для блока `_send_result_and_update_state` / billing warning logic (или создать новый тест-файл если его нет)
- [x] Task 1.2: Написать тест: `status="exhausted"`, `package_minutes=0`, `bonus_minutes=0` → ожидаем текст `⛔ Дневной лимит исчерпан`
- [x] Task 1.3: Написать тест: `status="exhausted"`, `package_minutes=30` → ожидаем мягкий текст (без `⛔`, со словами о пакетных минутах)
- [x] Task 1.4: Написать тест: `status="exhausted"`, `bonus_minutes=10` → ожидаем мягкий текст (без `⛔`)
- [x] Task 1.5: Написать тест: `status="warning"`, `package_minutes=0`, `bonus_minutes=0` → ожидаем текст `⚠️ Дневной лимит почти исчерпан`
- [x] Task 1.6: Написать тест: `status="warning"`, `package_minutes=30` → ожидаем мягкий текст (без `⚠️`)
- [x] Task 1.7: Написать тест: `status="ok"` → никакого сообщения не отправляется (текущее поведение не сломано)

### Verification

- [x] `TELEGRAM_BOT_TOKEN=test uv run pytest tests/unit/test_transcription_orchestrator.py -v` — новые тесты падают (RED)

## Phase 2: Implementation

Изменить логику предупреждений в `transcription_orchestrator.py`.

### Tasks

- [x] Task 2.1: В блоке `if status in ("warning", "exhausted"):` добавить переменную `has_extra_minutes = balance.package_minutes > 0 or balance.bonus_minutes > 0`
- [x] Task 2.2: Для `status == "exhausted"` разветвить сообщение:
  - Без пакетов (текущее): `⛔ Дневной лимит исчерпан!\n\nИспользовано: X из Y мин`
  - С пакетами (новое): `ℹ️ Дневной лимит исчерпан.\n\nИспользовано: X из Y мин дневного лимита. Остаток будет списан из ваших пакетных минут.`
- [x] Task 2.3: Для `status == "warning"` разветвить сообщение:
  - Без пакетов (текущее): `⚠️ Дневной лимит почти исчерпан!\n\nИспользовано: X из Y мин`
  - С пакетами (новое): `ℹ️ Дневной лимит почти исчерпан.\n\nИспользовано: X из Y мин. Когда лимит закончится, будут использоваться ваши пакетные минуты.`

### Verification

- [x] `TELEGRAM_BOT_TOKEN=test uv run pytest tests/unit/test_transcription_orchestrator.py -v` — все тесты зелёные (GREEN)

## Phase 3: Full CI Check

### Tasks

- [x] Task 3.1: `uv run ruff check src/`
- [x] Task 3.2: `uv run black --check src/ tests/`
- [x] Task 3.3: `uv run mypy src/`
- [x] Task 3.4: `TELEGRAM_BOT_TOKEN=test uv run pytest tests/unit/ -v`

### Verification

- [ ] Все CI проверки проходят без ошибок

## Final Verification

- [ ] Все acceptance criteria из spec.md выполнены
- [ ] Тесты проходят
- [ ] Линтеры чистые
- [ ] Типы верные
- [ ] Готово к PR

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
