# Вопросы к Константину (issue #140 + «на новую подписку — отдельная строка»)

## Контекст находок (по коду main и прод-БД)

1. Полного UNIQUE(user_id) в прод-БД **нет**. Есть partial unique index `ix_user_subscriptions_unique_active ON user_subscriptions(user_id) WHERE status='active'` (миграция e7a1b2c3d4f5). SQLite сообщает о его нарушении как `UNIQUE constraint failed: user_subscriptions.user_id` — это и есть ошибка из логов.
2. Код уже рассчитан на «историю строк»: `SubscriptionService.create_subscription` деактивирует активную и вставляет новую строку. Падение происходит, когда у юзера строка со статусом `active`, но `expires_at <= now` (истекла по времени, но крон `check_expired_subscriptions` ещё не перекрасил в `expired`): `get_active_subscription` фильтрует по `expires_at > now`, не находит строку → деактивации нет → INSERT новой `active` → нарушение partial index.
3. Итого под желаемую «отдельная строка на подписку» схема почти готова; нужен фикс выборки «деактивировать все active-строки юзера независимо от expires_at» + страховки.

## Вопросы

### В1. Расчёт expires_at новой строки при покупке поверх действующей подписки (того же тира)
Issue предлагает «expires_at += период». Но при отдельной строке есть два варианта:
- (а) stacking: новая строка `expires_at = max(now, старая.expires_at) + период` — оплаченные дни не сгорают, старая строка помечается cancelled/replaced;
- (б) с нуля: `expires_at = now + период`, остаток старой строки пропадает.
Какой вариант? Моя рекомендация — (а).

**Ответ К.:**

### В2. Покупка другого тира поверх активного
Подтверждаем текущую семантику: новая покупка другого тира — немедленный переход (старая → cancelled, новая active с now + период, без stacking между тирами)? Stacking только для того же тира?

**Ответ К.:**

### В3. Partial unique index `ix_user_subscriptions_unique_active`
Оставляем как страховку БД-уровня (ровно одна active-строка на юзера) — код обязан деактивировать старые до INSERT? Или убираем и полагаемся только на код?

**Ответ К.:**

### В4. Компенсации сгоревших платежей
В issue: «вручную компенсировано правкой БД». Нужен ли в рамках задачи reconciliation-скрипт/команда (найти succeeded-платежи провайдера без completed-покупки и выдать)? Или уже всё закрыто вручную и скрипок не надо?

**Ответ К.:**

### В5. Сопутствующее из issue — объём
Включаем ли в этот PR:
- добавить `ABANDONED` в `PurchaseStatus` enum;
- проверить, что `find_pending_purchase` (матчит только PENDING) не цепляет abandoned — по коду уже ок, добавить тест.
Всё в один PR или отдельно?

**Ответ К.:**

### В6. Статус старой строки при замене
Сейчас `deactivate_subscription` ставит `cancelled`. Для аналитики отличать «отменена пользователем» от «заменена новой покупкой»? Вариант: добавить статус `replaced` (или поле `replaced_by_subscription_id`). Нужно ли?

**Ответ К.:**
